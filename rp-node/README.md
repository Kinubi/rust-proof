# rp-node

`rp-node/` is the shared node-engine crate.

Its target role is:

- device-agnostic node behavior
- `no_std + alloc`
- sync logic, peer state, import orchestration, and runtime boundaries

This crate is currently only a skeleton. The real implementation work is still ahead.